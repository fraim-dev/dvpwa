# Example GitHub Actions workflow for Fraim Security Scan
# Copy this file to .github/workflows/security.yml in your repository

name: Security Scan with Fraim

on:
  pull_request:
    branches: [master]

jobs:
  security-scan:
    name: Fraim Security Analysis
    runs-on: ubuntu-latest
    
    permissions:
      contents: read
      actions: read
      security-events: write
      pull-requests: write
    
    steps:
      - name: Run Fraim Security Scan
        id: fraim-scan
        uses: fraim-dev/fraim-action@a15e85774620304b41e44c325c1afc70fad5fac1
        with:
          openai-api-key: ${{ secrets.OPENAI_API_KEY }}
          model: 'openai/gpt-5'
          workflow: 'code'
          workflow_args: |
            {
              "temperature": "1"
            }
